import { HowItWorksContainer } from '@features/howItWorks';

/**
 * How It Works page showing the FLAMES game explanation in a step-by-step format.
 */
export default function HowItWorksPage() {
  return <HowItWorksContainer />;
}
